<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nextpin.app.dao.CourseHomeReview2Dao">

    <select id="findCourseIdByUserIdAndCourseName" parameterType="map" resultType="string">
        SELECT courseId FROM course WHERE userId = #{userId} AND courseName = #{courseName} LIMIT 1
    </select>

    <insert id="insertCourse">
        INSERT INTO course (courseId, userId, nickname, courseName, color, regDate, modifyDate)
        VALUES (#{courseId}, #{userId}, #{nickname}, #{courseName}, #{color}, #{regDate}, #{modifyDate})
    </insert>

    <insert id="insertCourseDetail" parameterType="com.nextpin.app.dto.CourseDetailDto">
        INSERT INTO course_detail (location, x, y, visitDate, memo, userId)
        VALUES (
                   #{location},
                   #{x},
                   #{y},
                   #{visitDate},
                   #{memo},
                   #{userId}
               )
    </insert>

    <update id="updateCourseColorAndModifyDate">
        UPDATE course
        SET color = #{color}, modifyDate = now()
        WHERE courseId = #{courseId}
    </update>

    <select id="getCourseDetails" resultType="com.nextpin.app.dto.CourseDetailDto">
        SELECT *
        FROM course_detail
        WHERE courseId = #{courseId}
    </select>

    <select id="getCoordinatesByLocation" resultType="map">
        SELECT x, y
        FROM course_detail
        WHERE location = #{location}
    </select>

    <select id="isDuplicateCourseDetail" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM course_detail
        WHERE courseId = #{courseId} AND location = #{location}
    </select>

    <select id="getCourseDetailsByNameAndUser" resultType="com.nextpin.app.dto.CourseDetailDto">
        SELECT cd.*
        FROM course_detail cd
                 JOIN course c ON cd.courseId = c.courseId
        WHERE c.courseName = #{courseName} AND c.userId = #{userId}
    </select>

    <select id="isLocationExist" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM course_detail
        WHERE courseId = #{courseId} AND location = #{location}
    </select>

    <delete id="deleteCourseDetail">
        DELETE FROM course_detail
        WHERE courseId = #{courseId} AND location = #{location}
    </delete>

</mapper>
